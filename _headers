# css/js 는 파일명에 해시가 없어서, 배포 후에도 브라우저가 옛 파일을 계속 쓰면
# 새 HTML + 옛 CSS 조합으로 화면이 깨진다 (2026-08-04 랜딩에서 실제로 발생).
# max-age=0 + must-revalidate → 매번 조건부 요청(대부분 304)으로 항상 최신을 보장한다.
/css/*
  Cache-Control: public, max-age=0, must-revalidate

/js/*
  Cache-Control: public, max-age=0, must-revalidate
